(load "/Users/oliverfencott/Desktop/projects/carp-vscode/examples/simple-1.carp")

(doc Simple "Simple module documentation")
(defmodule Simple    
  (doc identity "Takes a string and returns a string")
  (defn identity [str]
    (the String str))

  (doc hello "Docs for hello")
  (def hello "Simple1.hello")
  ;; (def hello "world")

  (defn x [arg-aaa arg-bbb]
    (+ arg-aaa arg-bbb))

  (defmodule Two
    (def two-world 1))
  )